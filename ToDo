Kansa core:

Modules:
Can non-log related GUIDs be resolved to human readable values for Get-SvcTrigs.ps1
Browser cache/history collector
ipconfig /all
route print
Update the old Get-DNSCache method using ipconfig /displaydns to mimic Get-DNSClientCache output
Should rename the tlist modules, since they don't actually call tslist
Get rid of xml output wherever possible
Fix empty result issue with Get-PSProfiles.ps1

Analysis:
Get-ProcessByAge.ps1
Get-ProcessStack.ps1
Get-NetstatProcessStack.ps1